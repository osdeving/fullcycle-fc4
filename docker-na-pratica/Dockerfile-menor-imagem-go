FROM golang:1.21 AS builder
RUN echo 'package main; func main() { println("Hello, World!") }' > main.go && CGO_ENABLED=0 go build -ldflags="-s -w" -o /app main.go
FROM scratch
COPY --from=builder /app /app
ENTRYPOINT ["/app"]

